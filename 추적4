Interceptor.attach(Module.getExportByName("user32.dll", "SetWindowsHookExA"), {
    onEnter(args) {
        console.log("[HOOK] idHook:", args[0].toInt32());
        console.log("HookProc:", args[1]);
    }
});