Interceptor.attach(ptr("0x14000741F"), {
    onEnter(args) {
        console.log("R9 =>", this.context.r9);

        try {
            const str = Memory.readUtf8String(this.context.r9);
            console.log("R9 string =>", str);
        } catch(e) {
            console.log("읽기 실패", e);
        }
    }
});