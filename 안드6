Interceptor.attach(prctl, {
    onEnter(args) {
        const opt = args[0].toInt32();
        if (opt === 1398164801) {
            console.log(
                "[seccomp trigger]",
                "tid =", Process.getCurrentThreadId()
            );
        }
    }
});